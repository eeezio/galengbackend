<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.galeng.backend.repository.UserRepository">
    <select id="selectPasswordByAccount" resultType="java.lang.String" parameterType="java.lang.String">
        select password from users where account=#{account}
    </select>
    <select id="selectCgByAccount" resultType="com.galeng.backend.entity.Cg" parameterType="java.lang.String">
        select * from cg where Account=#{Account} and Gameid=#{gameid} order by Cgpage,Cgid
    </select>
</mapper>